import React from "react";
import styled from "styled-components";

function Header() {
  return <Title>Post code App</Title>;
}

export default Header;

const Title = styled.div`
  padding: 20px 0;
  text-align: center;
  font-size: 32px;
  font-weight: bold;
`;
